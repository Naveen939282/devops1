name: Python Test
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/Checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
        Python-version: "3.10"
      - name: Run test
        run: Python naveen.py